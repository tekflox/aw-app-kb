-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
pytest-cov>=5.0
pylint>=3.2
ruff>=0.7
pre-commit>=3.8
# tests/validate_manifest.py's own import — the shared release workflow
# installs this separately (app-release.yml:110-137) before this file, but
# the local pre-commit hook that calls the same script needs it here too.
jsonschema>=4.0
